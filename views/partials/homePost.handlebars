{{#each this}}
    <div class="card">
        <div class="card-title">
            <p>{{title}}</p>
            <p>Posted by {{user.name}} on {{format_date created_at}}</p>
        </div>
        <div class="card-description">
            <p>{{description}}</p>
        </div>
    </div>
{{/each}}