{{#each this}}
<div class="card">
    <div class="card-title">
    </div>
    <div class="card-description right">
        <p>{{comment}}</p>
        <p>- {{ user_id}}, {{ format_date createdAt}}</p>
    </div>
</div>
{{/each}}